package com.example.demo.infrastructure.config;

/**
 * 日志配置类
 * 如果需要自定义日志格式或配置日志相关的 Bean，可以在这里配置
 * Spring Boot 默认使用 logback，可以通过 logback-spring.xml 配置
 * 
 * 注意：当前为空配置类，如果不需要自定义日志配置，可以删除此类
 */
// @Configuration
public class LoggingConfig {
    
    // 如果需要自定义日志格式，可以在这里配置
    // Spring Boot 默认使用 logback，可以通过 logback-spring.xml 配置
}

